<%= form_for(@stinsfo) do |f| %>
  <% if @stinsfo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stinsfo.errors.count, "error") %> prohibited this stinsfo from being saved:</h2>

      <ul>
      <% @stinsfo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :place %><br>
    <%= f.text_field :place %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
